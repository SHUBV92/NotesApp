(function(exports) {
    function testNoteList() {
        var note = new NoteList();

        assert.isTrue(note.array === []);
        // assert.isTrue(note.add === note.text);

    }
    testNoteList()
 
}) (this);